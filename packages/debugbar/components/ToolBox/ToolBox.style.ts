import { Column } from "../common/flexs/Column";

import styled from "@emotion/styled";

const SC = {
  Wrapper: styled(Column)`
    justify-content: center;
    align-self: stretch;
  `,
};

export default SC;
